function twice (x)
  return 2.0 * x
end

--[[
The exercise asks to use

lua
dofile("ex1_2.lua")
twice(a number)

or directly in the shell command:
lua -lex1_2 -- and enter the interactive mode with the file loaded

--]]
